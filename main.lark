%import common.WS
%import common.NEWLINE
%ignore WS
%ignore NEWLINE

%import .grammar.assign (assign_stmt, value)
%import .grammar.condition (when_stmt, comparison, block)

?start: stmt*

?stmt: assign_stmt
     | when_stmt
